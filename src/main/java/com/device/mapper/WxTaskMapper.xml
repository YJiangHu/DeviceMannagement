<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.device.mapper.WxTaskMapper">

    <insert id="upTask" parameterType="com.device.po.WxTask">
        INSERT INTO task_manage(task_onm_id,task_onm_content,task_error,task_onmer_id)
        VALUES( #{onmId}, #{onmContent},#{isError},#{onmerId})
    </insert>
</mapper>